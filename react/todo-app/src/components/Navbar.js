const Navbar = ()=>{
    return(
        <nav className="navbar">
            <h2>Todo App...</h2>
            <div className="links">
                <a href="#">Home</a>
                <a href="#">Add New</a>
            </div>
        </nav>
    );
}

export default Navbar;